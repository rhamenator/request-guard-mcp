# Agent Channel Pointer

The shared Codex/Claude agent channel for this four-repo workspace lives in
the Python reference repo: `e:\ai-scraping-defense\.agent-channel\`
(`log.jsonl` + per-agent cursor files; protocol in its README.md).

Do not start a separate log here — append to and read from the shared one.
