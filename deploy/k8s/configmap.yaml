apiVersion: v1
kind: ConfigMap
metadata:
  name: mcp-config
  namespace: ai-defense
data:
  log_level: "info"
  redis_url: "redis://redis:6379"
  max_batch_size: "50"
  global_concurrency: "256"
  per_tool_timeout_secs: "30"
