# Clippy configuration
# Run: cargo clippy --all-targets --all-features -- -D warnings

# Deny pedantic lints that catch real bugs
cognitive-complexity-threshold = 30

# Allow common patterns
too-many-arguments-threshold = 8
